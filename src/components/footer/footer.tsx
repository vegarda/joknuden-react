import * as React from 'react';

import './footer.scss';

export default class Footer extends React.Component {
    public render() {
        return (
            <footer>footer</footer>
        )
    }
}